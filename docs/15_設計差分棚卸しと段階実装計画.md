# SeedShiftRadio 設計差分棚卸しと段階実装計画

## 1. 目的

本書は、設計書と現行実装を突き合わせ、実装済み範囲、設計差分、GitLab 作業 ID、依存関係、段階実装順を示す。

製品仕様は関係する `docs/00` から `docs/17` を正本とする。

作業状態、担当主体、優先度、完了確認は GitLab Project Issue / Label / Issue Board を正本とする。

本書は Issue の代替ではなく、複数 Issue を横断する実装順と設計上の依存関係を示す。

## 2. 棚卸し基準

| 項目 | 内容 |
|---|---|
| 棚卸し日 | 2026-07-12 |
| 基準 commit | `182153a390a1abae139ef6fe951fc33bd228b35e` |
| 基準 pipeline | GitLab `#44`、pipeline id `276`。必須 test / build / publish / security job は全件成功し、production deploy と GPU smoke は manual |
| Server 配置 | repo root を Server module として維持する |
| Web 配置 | `web/` |
| Worker 配置 | `workers/musicgen/` |
| Compose 配置 | `infra/compose/` |
| 設計書 | `docs/00` から `docs/17` |

実装状況は次の語で表す。

| 状態 | 定義 |
|---|---|
| 実装済み | 設計契約を満たすコードと自動 test があり、通常利用できる |
| 部分実装 | 中核経路はあるが、受け入れ条件または運用条件に残りがある |
| 未実装 | production で利用できる実装がない |
| 修正中 | 不具合修正を実装済みだが、必要な最終 gate が未完了である |

## 3. 現行実装の全体像

| 領域 | 現行実装 | 状態 | 主な残差分 |
|---|---|---|---|
| REST / SSE | Radio、queue、station、programming、letter、settings、monitor、assets、health、client capabilities、OpenAPI contract check を実装 | 実装済み | Web client と C# contract harness の自動差分検査 |
| Playout | Server 正本の session / queue、warmup / refill、playback event、fallback、先読み policy を実装 | 実装済み | 実 provider の長時間・縮退試験 |
| LLM script | `TemplateScriptProvider` に加え、JDK `HttpClient` ベースの Ollama / OpenAI 互換 adapter、strict structured JSON 検証、LETTER の安全要約と原文参照分離、試行単位 provider job、回復可能 error の Provider chain fallback、script から TTS asset までの orchestration と cache-first 追跡を実装 | 部分実装 | 実 Provider 長時間試験 |
| TTS | VOICEVOX、Irodori OpenAI 互換 adapter、VoiceProfile runtime 反映、provider chain、placeholder、日本語品質 golden test を実装 | 部分実装 | 実 provider 長時間試験、VoiceProfile UI |
| MusicGen | FastAPI worker、deterministic backend、Server submit / poll、fallback を実装 | 部分実装 | 実モデル backend、ACE-Step を含む運用接続 |
| Programming | template、rule、policy、preview、runtime planning、cache / replay 制御を実装 | 実装済み | 運用時の長時間回帰 |
| Letter | 投稿、idempotency、moderation state、採用、返信、放送履歴、LLM 安全要約、TTS control token 防御を実装 | 実装済み | 実 Provider との長時間放送品質は LLM / TTS 領域で追跡 |
| Settings | `config.json`、楽観 lock、import / export、接続 test を実装 | 部分実装 | schema migration、backup / restore |
| Monitor / audit | provider health、job、asset consistency、SSE 履歴 summary、stale provider job 回収、Provider job / 事前生成失敗の構造化運用ログ永続化を実装 | 部分実装 | 全監査イベントの永続化と retention |
| Web | `/`、`/letters`、`/settings`、`/monitor`、`/monitor/logs`、SSE reconnect、audio playback、同一 origin 管理 session / BFF / CSRF を実装 | 部分実装 | VoiceProfile / 読み辞書 UI |
| Infra / CI | Podman Compose、GitLab job 分割、container build / deploy / smoke を実装 | 実装済み | production 管理 UI smoke、GPU 実モデル smoke |
| C# 共有契約 | REST、SSE、`SpeechDirective`、client capabilities の設計を定義 | 部分実装 | Native Client 向け contract harness |

## 4. 今回確認した設計上の問題

### 4.1 production の Web 管理認証を成立させた

Server の管理 API が `X-Admin-Token` を必須とする共有契約は維持した。

`P0-11` で Web 管理用の login / session / logout、署名済み `HttpOnly` Cookie、session 固有 CSRF token、同一 origin `/api-proxy` を実装した。BFF は browser 由来の credential header と Cookie を upstream へ転送せず、認証済み ADMIN request だけに server-side の `SEEDSHIFT_ADMIN_TOKEN` を注入する。

`NEXT_PUBLIC_SEEDSHIFT_ADMIN_TOKEN`、`NEXT_PUBLIC_ADMIN_TOKEN`、直接 Server URL を指定する browser API base は削除した。production compose と deploy env は `SEEDSHIFT_WEB_ADMIN_PASSWORD`、`SEEDSHIFT_WEB_SESSION_SECRET`、`SEEDSHIFT_ADMIN_TOKEN` を Web runtime だけへ渡し、管理 session smoke で未認証拒否、CSRF、認証済み proxy、logout を確認する。

`P0-13` では GitLab Web E2E で顕在化した正当な login request の `403` を修正する。同一 origin 判定は Next.js 内部 URL ではなく、`Origin` と利用者から見える protocol / host を比較する。reverse proxy の forwarded header と直接接続の両方を unit test で、login / session と cross-origin 拒否を Playwright で固定する。診断へ password、session Cookie、CSRF token、管理 API 用トークンは出さない。

### 4.2 TTS の最終縮退が voice hint により外部 adapter へ戻る

`seedshift-placeholder` provider の adapter 推定より `SpeechDirective.voiceHint` が優先されていた。

このため VOICEVOX 失敗後の placeholder fallback でも VOICEVOX HTTP 接続を再実行し、queue warmup が完了しなかった。

`P0-10` で provider key の最終縮退を優先するよう修正し、回帰 test を追加した。

### 4.3 外部 Provider 仕様の更新が設計書へ反映されていない

Irodori-TTS-Server は `stream_format=sse` による chunk-level SSE を提供している。

現行 SeedShiftRadio adapter は完成 WAV を保存するため、upstream capability と adapter の有効状態を分けて記述する。

`P0-02b` では chunk-level SSE を現行 playout 契約へ採用せず、完成 WAV asset の先行生成を維持する判断とした。

### 4.4 API 契約の drift 検出を追加した

Controller、生成 OpenAPI、`docs/02` に分散していた endpoint、DTO、認証区分を `P1-18` で比較対象へ統合した。

`ApiContractTests` は `/api/openapi` の SHA-256 snapshot、Spring MVC handler の `X-Admin-Token` header、`api-auth-matrix.json`、`docs/02` の認証表を照合する。

GitLab CI の `api-contract` job で `./gradlew apiContractTest` を独立実行する。手書き Web client と将来の C# client fixture の比較は `P2-12` で追加する。

### 4.5 運用上必要な永続化と復旧が不足する

監査 summary は process memory 上の SSE 履歴に依存する。

`config.json` は schema migration と raw backup / restore を持たない。

読み辞書は hard-coded 共通語だけで、global / station / persona 層を運用できない。

停止した process の `provider_job=RUNNING` は、設定可能な stale 閾値と batch 上限を持つ定期回収処理で `FAILED / PROVIDER_INTERRUPTED` へ確定する。
回収処理は request 本文を永続化せず同じ要求を自動再送しない。
必要な生成物は上位の queue warmup / refill / planning が新しい論理要求として補充し、`runOnce` の走査・回収件数、既存 monitor summary と `provider.job.failed` で状態を確認できる。

これらを `P1-19` から `P1-22` に分割する。

### 4.6 設計・リポジトリ衛生が現状とずれている

旧 `docs/14` と `docs/15` は実装済み作業を未実装とし、現在の GitLab-first 運用と矛盾する固定サブエージェント割当を残していた。

また、生成物が Git 管理され、ローカル起動ガイドが ignore されていた。

設計書は今回更新し、生成物整理とガイド正本化を `P2-11` で追跡する。

### 4.7 Web 依存に既知脆弱性が残っている

Next.js `15.5.14`、Vitest、Vite、PostCSS、`ws` に production または development の既知 advisory が残っていた。

`P0-12` で同一 major / minor 内の修正版と最小 override へ更新し、`npm audit` を 0 件にした。

### 4.8 Provider error / fallback 契約を共通化した

`P0-05` で LLM、TTS、Music Generation の失敗を共通 Provider error code へ揃えた。

Provider chain の fallback は `PROVIDER_UNREACHABLE`, `PROVIDER_TIMEOUT`, `PROVIDER_BAD_RESPONSE`, `PROVIDER_RESOURCE_EXHAUSTED` と、TTS 固有の `VOICE_REF_NOT_FOUND`, `VOICE_CONSENT_REQUIRED` だけで許可する。
安全性ポリシーによる拒否を含む `PROVIDER_REJECTED`、認証失敗の `PROVIDER_AUTH_FAILED`、中断の `PROVIDER_INTERRUPTED` では同じ要求を別 Provider へ自動送信しない。

論理生成ジョブの `provider_job.status` は `FAILED` または `SUCCEEDED` とし、`DEGRADED` は Provider health と playout state で表す。
TTS は Provider 試行を個別 job として追跡し、Music Generation の Provider chain は最終 Provider と論理ジョブの最終結果を一つの job に残す。
外部 Provider または Worker の未知 error code は `PROVIDER_BAD_RESPONSE` へ正規化する。

### 4.9 管理トップと局別コンテンツ運用を追加した

局、番組、Provider、queue、生成 job の状態が複数画面へ分散し、局ごとの番組数・生成 asset 数量や事前生成の進捗を一画面で判断できなかった。
`P1-23` では `/settings` を管理ダッシュボードへ変更し、局別コンテンツ台帳、`/settings/content` のオフエア事前生成、局・asset 種別を指定する事前生成 payload 削除を追加した。
事前生成は `purpose=PRE_GENERATION` の session を使い、ライブ radio status、現在番組、再生 queue、SSE、レター採用状態を変更しない。
削除は `PRE_GENERATION` かつ archive 対象外の payload だけを eviction と同じ tombstone 方式で処理し、番組、queue、Provider job、asset record、監査履歴を保持する。

2026-07-28 の production 障害調査では、ACE-Step `/health` が HTTP 200 でも `models_initialized=false`, `llm_initialized=false` のまま `/release_task` を 400 で拒否し、SeedShiftRadio 側では `PROVIDER_REJECTED` へ正規化されていたことを確認した。Server health は初期化状態を解析して `DOWN` とし、`MUSIC_AI` の新規計画を避けるようにした。実生成を再開するには Issue `#11` で ACE-Step 起動時の model / LM 初期化と container healthcheck を永続化する。

2026-07-29 の再調査では、RTX 3080 Ti 12 GB の production ACE-Step が `acestep-v15-turbo` と `acestep-5Hz-lm-0.6B` を正常に初期化している一方、SeedShiftRadio の既定 profile が 24 GB 級を想定する `ace-ja-xl-quality` へ変更されていたため、実ロード状態と一致する `ace-ja-fast` へ戻した。また、ACE-Step `v0.1.8` の OpenAI 互換 `/v1/models` が `id=acestep/acestep-v15-turbo`, `name=ACE-Step acestep-v15-turbo` を返すため、表示名または namespace 付き ID の末尾が profile model と一致する場合も利用可能と判定する回帰 test を追加した。ACE-Step 側 compose の healthcheck を初期化状態まで検査する残作業は Issue `#11` で継続する。

## 5. GitLab 作業タスク

### 5.1 既存 Issue

| 作業 ID | Issue | 状態 | 実装範囲 | 完了条件の要点 |
|---|---:|---|---|---|
| `P0-01` | `#3` | 確認待ち | Ollama / OpenAI 互換 LLM adapter | strict structured JSON から安全な `GeneratedScript` を生成し、試行単位 job、fallback、非露出を focused test で固定した |
| `P0-02b` | `#4` | 確認待ち | Irodori TTS adapter | provider options、参照音声、同意、station scope、provider 優先順、streaming 非採用を runtime と focused test で固定した |
| `P0-02c` | `#5` | 確認待ち | VoiceProfile と参照音声 metadata | `V12`、`VoiceProfileEntity`、`StationAdminService` で局 scope、同意必須、安全な参照、path / 個人名非露出を固定した |
| `P0-03` | `#6` | 確認待ち | script から TTS asset までの orchestration | `SpeechAssetGenerationService`、script / TTS cache-first、試行・cache hit job、`scriptAssetId` / `scriptProviderJobId` を focused test で固定した |
| `P0-04` | `#7` | 確認待ち | LETTER の安全な要約と放送台本 | `LetterBroadcastContentFactory` で命令文と個人情報候補を除去・代替し、安全要約と原文参照を分離した |
| `P0-05` | `#8` | 確認待ち | Provider error / fallback 契約 | 共通 error code、fallback 可否、health / playout の `DEGRADED` 境界を固定した |
| `P0-05a` | `#9` | 確認待ち | 日本語読み・style safety test | かな補正、allowlist、letter 由来 control token 遮断を `JapaneseBroadcastQualityGoldenTests` で固定した |
| `P0-06` | `#10` | 着手可能 | MusicGen 実モデル backend | deterministic 以外の backend で再生可能な WAV を生成する |
| `P0-09` | `#11` | 作業中 | ACE-Step / worker 運用接続 | compose、settings、health、runbook、smoke が一つの接続方式で動く |
| `P2-06` | `#12` | 着手可能 | Spring Boot 更新方針 | `4.1.0` 追随可否、互換性、保守期間、rollback を決定する |
| `P0-10` | `#23` | 確認待ち | placeholder TTS fallback | `server-docker` を含む Radio API 回帰 test が成功する |

### 5.2 追加 Issue

追加 Issue は `.gitlab/scripts/bootstrap-work-management.ps1` から GitLab へ登録する。

2026-07-12 の棚卸し結果を `#24` から `#32` として登録済みである。

| 作業 ID | Issue | 優先度 | 状態 | 依存 | 実装範囲 | 完了条件の要点 |
|---|---:|---|---|---|---|---|
| `P0-11` | `#24` | P0 | 確認待ち | なし | Web admin session、BFF、CSRF、compose、E2E | browser bundle / storage / log に管理トークンを出さず production 管理 API を利用できる |
| `P0-12` | `#33` | P0 | 確認待ち | なし | Next.js / Vitest / transitive dependency、npm audit | production / development の既知脆弱性が 0 件で、Web test と build が成功する |
| `P0-13` | `#34` | P0 | 確認待ち | `P0-11`（確認待ち） | Web login same-origin、E2E 診断、回帰 test | 公開 host と内部 URL が異なる GitLab E2E でも正当な login が成功し、cross-origin は `403` のまま拒否する |
| `P0-14` | `#36` | P0 | 確認待ち | なし | 番組単位の連続再生、playback event 冪等性、既定トーク・音楽編成、音楽 placeholder | 1 回の再生操作で同一番組を末尾まで再生し、404 / 409 や無音停止を起こさず、トーク後に曲として聞ける音楽を流す |
| `P1-17` | `#25` | P1 | 着手可能 | `P0-02b`（確認待ち） | VoiceProfile 管理 UI | voice、style、同意参照を局 scope 内で安全に編集できる |
| `P1-18` | `#26` | P1 | 確認待ち | なし | OpenAPI、認証マトリクス、CI | `ApiContractTests` と `api-contract` job が Controller、schema、`docs/02` の差分を検出する |
| `P1-19` | `#27` | P1 | 着手可能 | なし | audit migration、API、retention | 再起動後も redacted audit event を検索できる |
| `P1-20` | `#28` | P1 | 着手可能 | なし | config migration、backup、restore | schema 更新失敗時に既知の設定へ rollback できる |
| `P1-21` | `#29` | P1 | 着手可能 | なし | 読み辞書 DB / API / UI | global、station、persona の優先順位と運用編集を test できる |
| `P1-22` | `#30` | P1 | 確認待ち | なし | provider job cleanup / retry | stale `RUNNING` を `FAILED / PROVIDER_INTERRUPTED` へ確定し、通常の queue 補充へ安全に収束する |
| `P1-23` | `#35` | P1 | 作業中 | なし | 管理ダッシュボード、局別コンテンツ台帳、オフエア事前生成・削除 | 全体状況と局別保有量を管理トップで確認し、ライブ再生を変えず番組・台本・音声・曲を事前生成し、事前生成 payload だけを種別指定で削除できる |
| `P2-11` | `#31` | P2 | 確認待ち | なし | `.gitignore`、`HELP.md`、追跡生成物 | checkout が生成物で汚れず、Podman 前提の起動手順を追跡できる |
| `P2-12` | `#32` | P2 | 未着手 | `P1-18` | C# 共有 contract harness | REST、SSE、`SpeechDirective`、reconnect の互換 fixture が CI で動く |

## 6. 実装済みとして維持する項目

次の旧作業 ID は実装済みであり、新しい Issue を作らない。

| 作業 ID | 実装済み内容 |
|---|---|
| `P0-02a` | VOICEVOX TTS adapter と WAV asset |
| `P0-05` | Provider error code、fallback 可否、provider job と health / playout の縮退状態境界 |
| `P0-07` | Server / worker の MusicGen contract test |
| `P0-08` | MusicGen 失敗時の既存 asset / jingle 縮退 |
| `P1-01` から `P1-06` | queue preparation、ahead count、pre-generation、composition、replay share、preview / runtime 整合 |
| `P1-07` から `P1-10` | asset eviction、archive promotion、metrics、file / DB consistency |
| `P1-11` から `P1-15` | Vitest、Playwright、settings import / export、station / template / policy UI、monitor UI |
| `P2-01` から `P2-05` | CI 分割、structured log、秘密値 review、runbook、Server 管理 API 認証 |
| `P2-07` から `P2-10` | repo root Server 方針、compose、環境例、統合 smoke |

旧 `P1-16` の「Web から secret 値を直接扱わない」は、より明確な `P0-11` へ置き換え、同一 origin BFF session 方式として実装した。

## 7. 段階実装計画

### 7.1 Gate 0: 既存再生経路を安定化する

1. `P0-10` の `server-docker` を再実行する。
2. Tune、queue warmup、playback event、play history、monitor summary が回復したことを確認する。
3. 検証結果を GitLab `#23` へ記録する。

この Gate が失敗している間は、fallback に関係する大きな provider 変更を統合しない。

### 7.2 Gate 1: production 管理面と契約 drift を止める

1. `P0-11` で Web 管理 session と CSRF 境界を実装した。production pipeline と HTTPS 利用者経路で smoke を継続する。
2. `P0-12` で Web dependency advisory を解消し、`npm audit` を release gate にする。
3. `P1-18` で OpenAPI と認証マトリクスの CI gate を追加する。
4. `/letters`、`/settings`、`/monitor` の未認証拒否と認証済み操作を production 相当 E2E で確認する。

`P0-11` と `P1-18` は実装・focused test 済みであり、既存 Server API 契約と Web BFF 専用認証 endpoint の境界を維持する。

### 7.3 Gate 2: 実 LLM / TTS 放送品質を完成する

`P0-05` の共通 error / fallback 契約は固定済みである。

`P0-02c` の VoiceProfile metadata と `P0-05a` の日本語品質 guard は実装・検証済みで確認待ちである。

`P0-01`、`P0-02b`、`P0-03`、`P0-04` は実装と focused test を完了し、確認待ちである。

後続 adapter は `P0-05` の共通分類を使い、adapter ごとの例外分類を増殖させない。

`P0-03` では script 生成と TTS audio 生成の責務を明示 service で接続し、両 asset の cache key、cache hit job、成功 job と相互追跡 metadata を固定した。`P0-04` では `LetterBroadcastContent` を放送用要約と原文参照 ID に分け、HTTP LLM request と定型 fallback から原文本文を除外し、`LETTER_SUMMARIZED` で追跡できるようにした。

VoiceProfile と同意 metadata の永続化・局 scope、および Irodori TTS runtime 反映は固定済みである。`P1-17` は `P0-02b` が確認待ちへ進んだため着手可能である。

### 7.4 Gate 3: 実音楽生成と運用接続を完成する

1. `P0-06` で実モデル backend と resource limit を実装する。
2. `P0-09` で compose、settings、health、deploy、runbook を同じ provider profile に接続する。
3. GPU smoke、worker down、timeout、fallback asset、再起動後 job 状態を確認する。

### 7.5 Gate 4: 運用耐久性を上げる

`P1-19`、`P1-20`、`P1-21` は独立して着手できる。

`P1-22` は完了した。
stale `RUNNING` を `PROVIDER_INTERRUPTED` で失敗確定し、自動再投入せず、上位 queue planning の新規論理要求で回復する。
前提としていた `P0-05` の共通 error / retry 分類は固定済みである。

`P1-23` は実装とローカル回帰確認を完了し、commit 後の確認待ち移行を残して作業中である。
管理トップは既存 monitor summary と局別コンテンツ台帳を集約し、事前生成はオフエア session でライブ再生から分離する。削除はこのオフエア事前生成 payload だけに限定し、履歴行と archive を保持する。

再起動、部分書き込み、古い schema、stale job、秘密値 redaction を failure injection test に含める。

### 7.6 Gate 5: 保守性と将来 Client 契約を固定する

1. `P2-06` で Spring Boot `4.1.0` 追随判断を行う。
2. `P2-11` で生成物とローカル起動ガイドを整理する。
3. `P1-18` 完了後に `P2-12` の C# contract harness を追加する。

## 8. 検証 gate

| 変更領域 | 必須検証 |
|---|---|
| Server domain / API | focused JUnit、Docker 不要全 test、該当時 `server-docker` |
| DB / Flyway | migration test、既存 data 互換、PostgreSQL Testcontainers |
| Web | typecheck、Vitest、該当 route の Playwright、browser bundle の secret scan |
| SSE / playback | reconnect、`Last-Event-ID`、重複 event、audio event idempotency |
| Provider | success、timeout、401 / 503、bad response、fallback、secret redaction |
| Worker | contract test、実 backend smoke、resource limit、down 時縮退 |
| Config / backup | migration、partial failure、restore、権限、secret reference |
| CI / compose | YAML validation、container build、compose config、smoke、rollback |
| Shared client contract | OpenAPI snapshot、SSE fixture、SpeechDirective、capability negotiation |

## 9. 今回の検証状況

| 検証 | 結果 |
|---|---|
| `HttpTtsProviderTests` | 成功 |
| Docker 不要 Server 全 test | 成功 |
| WSL Ubuntu `dockerTest` | 成功 |
| Web typecheck | 成功 |
| `P0-11` Web focused test | 34 件成功。session 署名、CSRF、ADMIN / PUBLIC 分類、browser credential 除去、server token 注入を確認した |
| Web Vitest | 43 件成功 |
| Web production build | 成功 |
| Web `npm audit` | production / development とも 0 件 |
| MusicGen worker test | 5 件成功 |
| `HttpScriptProviderTests` | 7 件成功。Ollama / OpenAI 互換 mapping、Bearer secret、strict JSON、401 / 503 / timeout / 接続不可、raw response 非露出を確認した |
| `ScriptGenerationServiceProviderFallbackTests` | 2 件成功。回復可能 error の次 Provider 試行、非回復 error の外部 chain 停止、定型台本 fallback、試行単位 job を確認した |
| `P0-03` focused test | `ScriptGenerationServiceProviderFallbackTests` と `AssetServiceTests` が成功。script / TTS cache hit、Provider 非呼び出し、現在 job への clone、script asset / job から audio asset への追跡を確認した |
| `P0-04` focused test | `LetterBroadcastContentFactoryTests`、`TemplateScriptProviderTests`、`HttpScriptProviderTests`、`ScriptGenerationServiceProviderFallbackTests` が成功。命令文・個人情報候補・TTS control token の除去、安全要約と原文参照の分離、HTTP request / 定型 fallback の原文非依存、`LETTER_SUMMARIZED` 付与を確認した |
| `P1-22` focused test | `ProviderJobServiceTests`、`ProviderJobRecoveryJobTests`、`MonitorServiceTests` が成功。stale 判定、batch、条件付き更新、競合時の重複 event 防止、自動再投入なし、回収 counter、monitor の `PROVIDER_INTERRUPTED` 分類、秘密情報非露出を確認した |
| Docker 不要 Server 全 test（P1-22 統合後） | 成功 |
| `P1-22` PostgreSQL integration test | `SeedShiftRadioApplicationTests` が成功。stale `RUNNING` だけを条件付き更新し、閾値内の `RUNNING` と `QUEUED` を変更せず、再走で重複更新しないことを確認した |
| Docker 不要 Server 全 test（P0-03 統合後） | 成功 |
| Docker 不要 Server 全 test（P0-01 統合後） | 成功 |
| API contract test（P0-01 統合後） | 成功 |
| `P1-23` focused Server test | `ManagementControllerTests`、`ManagementServiceTests`、`PlayoutSessionEntityTests`、`GenerateMusicJobTests` が成功。管理認証、局別集計、入力制約、オフエア session、ライブ同期非実行を確認した |
| Docker 不要 Server 全 test（P1-23 統合後） | 成功 |
| `P1-23` Docker integration test | 成功。Flyway `V13`、`purpose=PRE_GENERATION` の番組数、局 scope の有効テンプレート数、eviction 済み payload を除く asset 件数・容量の PostgreSQL query を確認した |
| API contract test（P1-23 統合後） | 成功。管理 API 3 operation と DTO schema、認証マトリクス、`docs/02`、OpenAPI SHA-256 snapshot の一致を確認した |
| Web Vitest（P1-23 統合後） | 81 件成功。管理 API client、proxy policy、局別容量表示を確認した |
| settings Playwright（P1-23 統合後） | 11 件成功。管理トップのカテゴリー遷移、局別コンテンツ台帳、オフエア事前生成 request と既存設定編集を確認した |
| Web production build（P1-23 統合後） | 成功。`/settings` と `/settings/content` の production route 生成を確認した |
| 2026-07-28 ACE-Step readiness / 局別削除 focused Server test | `ProviderHealthServiceTests`, `MusicGenWorkerGatewayTests`, `GeneratedAssetServiceTests`, `ManagementServiceTests`, `ManagementControllerTests` が成功。初期化未完了の `DOWN` 判定、現行 model catalog、tombstone、管理認証、監査イベントを確認した |
| Docker 不要 Server 全 test（2026-07-28 統合後） | `--rerun-tasks --no-build-cache` で成功 |
| API contract test（2026-07-28 統合後） | 成功。管理 API 4 operation、認証マトリクス、`docs/02`、OpenAPI SHA-256 `f2cf3dd9deb112ee86a7ea2666556b30c9ceefcc9033c40315adf9e32298854a` の一致を確認した |
| PostgreSQL deletion query integration test（2026-07-28 追加） | `PRE_GENERATION`、局、種別、payload 有無、archive 除外を確認する test を追加。現行 Windows 環境の `dockerTest` は container runtime 未検出のため skip され、CI / Docker 利用環境での実行を残す |
| Web typecheck / Vitest（2026-07-28 統合後） | typecheck 成功、87 件成功。削除 API client と ACE-Step 初期化状態の安全な表示を確認した |
| 局別削除 Playwright（2026-07-28 統合後） | Chromium 1 件成功。対象種別選択、確認、`AUDIO` / `MUSIC` request、削除件数・回収容量表示を確認した |
| Web production build（2026-07-28 統合後） | compile、型検査、20 route の static generation まで成功。standalone trace copy は Windows の symlink 権限 `EPERM` で未完了 |
| GitLab pipeline `#44` の Server / Web / worker / container / security jobs | 必須 job 全件成功 |
| GitLab pipeline `#44` の `server-docker` | 成功 |
| GitLab pipeline `#44` の Web E2E | 11 件成功 |
| production smoke / GPU smoke | production変更とGPU実行を伴うmanual gateのため未実行 |

詳細な再試験結果と判定条件は `docs/14_レビュー対応・再試験計画.md` を参照する。

## 10. 更新ルール

- Issue を追加、分割、完了した場合は、本書の作業 ID、依存関係、段階実装順を更新する。
- API、DTO、SSE、認証区分を変えた場合は `docs/02` と必要に応じて `docs/10` を更新する。
- Web route、state、player、管理 session を変えた場合は `docs/03` を更新する。
- DB、config、asset、cache、backup を変えた場合は `docs/04` を更新する。
- playout、queue、fallback、JobRunr を変えた場合は `docs/05` と `docs/11` を更新する。
- LLM、TTS、読み辞書、persona、speech directive を変えた場合は `docs/06`、`docs/07`、`docs/08` を確認する。
- MusicGen worker を変えた場合は `docs/09` を更新する。
- CI、container、deploy、rollback、smoke を変えた場合は `docs/16`、`docs/17` と必要に応じて `docs/11` を更新する。
- 完了判定はコード、設計書、focused test、GitLab 記録が揃った時だけ行う。
